name: firebase_analytics
description:
  Flutter plugin for Google Analytics for Firebase, an app measurement
  solution that provides insight on app usage and user engagement on Android and iOS.
homepage: https://firebase.google.com/docs/analytics
repository: https://github.com/firebase/flutterfire/tree/main/packages/firebase_analytics/firebase_analytics
version: 12.4.2
publish_to: none
topics:
  - firebase
  - analytics
  - measure
  - insights

false_secrets:
  - example/**

environment:
  sdk: '^3.12.0'
  flutter: '>=3.44.0'

dependencies:
  firebase_analytics_platform_interface:
    git: https://github.com/kumsumit/firebase_analytics_platform_interface.git
  firebase_analytics_web:
    git: https://github.com/kumsumit/firebase_analytics_web.git
  firebase_core:
    git: https://github.com/kumsumit/firebase_core.git
  firebase_core_platform_interface: 
    git: https://github.com/kumsumit/firebase_core_platform_interface.git
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: io.flutter.plugins.firebase.analytics
        pluginClass: FlutterFirebaseAnalyticsPlugin
      ios:
        pluginClass: FirebaseAnalyticsPlugin
      macos:
        pluginClass: FirebaseAnalyticsPlugin
      web:
        default_package: firebase_analytics_web
